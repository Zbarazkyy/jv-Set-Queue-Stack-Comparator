import set_hashSet.SymmetricDifference;
import set_treeSet.SortLetters;

import java.util.Arrays;
import java.util.HashSet;
import java.util.Set;

public class Main {
    public static void main(String[] args) {
//        Set<Integer> setA = new HashSet<>(Arrays.asList(1, 2, 3));
//        Set<Integer> setB = new HashSet<>(Arrays.asList(0, 1, 2));
//        SymmetricDifference<Integer> xxx = new SymmetricDifference<>();
//        System.out.println(xxx.symmetricDifference(setA, setB));

        SortLetters yyy = new SortLetters();
        System.out.println(yyy.getUniqueCharacters("ur-BvT?^ ra w; p"));
        System.out.println(Thread.currentThread().getStackTrace());

    }
}
